If you are concerened over the fact this script gets the IP address its because it shows the user where the API endpoint is running. 

P.S A "Full stack developer" claimed this was an IP Grabber once! When its open source lmao.
